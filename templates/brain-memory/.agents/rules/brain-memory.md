# Shared Brain Memory

@../../AGENTS.md

Use the repository's shared Brain Memory instructions from `AGENTS.md`.

For memory-backed work, begin with `memory/INDEX.md` and load only relevant memory files. Do not preload the entire memory tree.
